# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.7, 3.8, 3.9]

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        $CONDA/bin/conda install -c conda-forge mamba -y
        $CONDA/bin/mamba env update --file environment.yml --name base
        $CONDA/bin/pip install -r dev-requirements.txt
        $CONDA/bin/pip install .
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        $CONDA/bin/flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        $CONDA/bin/flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Test with pytest
      run: |
        # Symlink the proj data to the pyproj data directory (the proj-data will otherwise not always be found)
        ln -s $PROJ_LIB/* $CONDA_DEFAULT_ENV/lib/python{{ matrix.python-version }}/site-packages/pyproj/proj_dir/share/proj/
        $CONDA/bin/pytest -rA
